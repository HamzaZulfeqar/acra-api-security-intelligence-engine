# Sprint 15 — Release Promotion

**Date:** 2026-09-26  
**Branch:** `s15-release-promotion`  
**Source:** Sprint 14 hardened head `88f2a75f190f6e8853609db2dc9d73929434ad0d`  
**Current source version:** `0.3.0-rc1`  
**State:** PREPARED_BLOCKED

## Objective

Move the technically hardened ACRA release candidate into a guarded promotion process without publishing, tagging,
licensing or version-promoting the project implicitly.

## Implemented promotion controls

- machine-readable `release/promotion-decision.json`;
- explicit license gate;
- explicit version/publication-path gate;
- Sprint 13 frozen-research lock;
- Sprint 14 ancestry/revalidation;
- direct-dependency allowlist;
- prohibition on custom Maven repositories, system-scoped dependencies and SNAPSHOTs;
- Maven dependency inventory;
- complete Sprint 14 regression/build/package revalidation;
- promotion CI artifact;
- draft release-promotion pull request.

## Canonical preparation evidence

Promotion-preflight run `36195798931` — SUCCESS.

Measured preflight:
- state: `PREPARED_BLOCKED`;
- license gate: blocked as expected / unresolved;
- version decision gate: blocked as expected / unresolved;
- Sprint 14 ancestry: PASS;
- frozen Sprint 13 research: PASS;
- dependency contract: PASS;
- core direct dependencies: 0;
- extension direct dependencies: 2;
- custom Maven repositories: 0;
- system-scoped dependencies: 0;
- SNAPSHOT dependencies: 0;
- Maven dependency inventory: PASS;
- Sprint 14 executable regression chain: PASS;
- Maven build/package: SUCCESS;
- release packaging/checksums/repeatability: PASS;
- promotion artifact upload: PASS.

The exact revalidated candidate generated by that CI run had:
- JAR SHA-256: `37f459f0dafe752960d3a9661984c84aa7e477dfc3c4f100e5406e8c411d0014`;
- bundle SHA-256: `7fe80361a0ede3d3c40fccb2f90d267ab9f3100e4f01222822100754ef6b569c`.

These hashes identify that exact CI build. Cross-build Maven JAR bit-reproducibility is not claimed.

## PR security evidence

PR-mode Security Checks run `36195804280`:
- CodeQL: PASS;
- Gitleaks: PASS;
- GitHub Dependency Review: UNAVAILABLE.

Dependency Review did not report a vulnerable dependency. It failed before analysis because GitHub reported that the
repository Dependency Graph is not enabled.

The promotion-specific dependency contract/inventory gate remains PASS. GitHub-native Dependency Review can be restored
by enabling Dependency Graph in repository security settings.

## Draft promotion PR

Draft PR #4:

`Release promotion: ACRA 0.3.0 candidate`

The PR must remain draft/not merge-ready until the explicit owner decisions below are complete.

## Remaining owner decisions

### 1. Software license — REQUIRED

Current state: **UNRESOLVED**.

No public distribution authorization is inferred.

### 2. Version/publication path — REQUIRED

Choose exactly one:
- keep `0.3.0-rc1` and publish it as a GitHub prerelease; or
- promote to `0.3.0` and rerun all promotion gates against the stable-version commit.

## Completion boundary

Sprint 15 preparation is complete, but release publication is not.

The next state transition requires explicit user decisions:

`PREPARED_BLOCKED → READY_FOR_PROMOTION`

Only after READY_FOR_PROMOTION is validated may tag/release publication be considered.
