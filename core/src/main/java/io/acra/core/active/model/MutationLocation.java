package io.acra.core.active.model;

public enum MutationLocation {
    PATH,
    QUERY,
    HEADER,
    COOKIE,
    BODY,
    RESOURCE,
    IDENTITY,
    ROLE,
    TENANT,
    URI_REPRESENTATION,
    METHOD
}
