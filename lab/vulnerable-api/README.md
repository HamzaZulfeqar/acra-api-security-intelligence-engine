# Vulnerable ACRA-Lab basic API

Sprint 2 implements the executable fixture at `lab/vulnerable-api/basic-api/server.py` using the shared service logic under `lab/common/basic_api.py`.

The fixture intentionally omits selected authorization checks for later controlled research. Sprint 2 does not exploit or classify those conditions as vulnerabilities.
