# Secure ACRA-Lab basic API

Sprint 2 implements the executable fixture at `lab/secure-api/basic-api/server.py` using the shared service logic under `lab/common/basic_api.py`.

The secure fixture enforces the basic tenant/owner ground-truth policy used for controlled integration work. Sprint 2 uses benign same-tenant traffic only.
